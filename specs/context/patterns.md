# Codebase Patterns

> Auto-generated by archaeologist.py
> These patterns are IMMUTABLE. The coding agent must follow them exactly.

---

I've analyzed the codebase and created a comprehensive patterns document at `specs/context/patterns.md`. Here's a summary of the key findings:

## Key Conventions Extracted

### Naming Conventions
- **Files**: `snake_case.py` for Python, `test_<feature-id>.spec.ts` for tests
- **Functions**: `snake_case` with verb-first naming (`create_checkpoint`, `verify_feature`)
- **Classes**: `PascalCase` with dataclass preference
- **Constants**: `UPPER_SNAKE_CASE`
- **Feature IDs**: `kebab-case` (`auth-001-email-login`)

### Import Patterns
- PEP 8 ordering: standard library → third-party → local
- Always use `pathlib.Path`, never `os.path`
- Modern type hints: `list[str]` not `List[str]`

### Required Libraries
- **Python**: Zero external dependencies (standard library only)
- **Node.js**: `@playwright/test` for testing, `@anthropic-ai/claude-code` for CLI

### Forbidden Patterns
- No `os.path` (use `pathlib`)
- No bare `except:` clauses
- No `List[str]` legacy type hints
- No global mutable state
- No silent failures
- No hardcoded paths in functions

### Testing
- Location: `tests/e2e/test_<feature-id>.spec.ts`
- Framework: Playwright with `describe`/`test` blocks
- Structure: Arrange/Act/Assert pattern

### Git Commits
- Prefixes: `PASSING:`, `FAILING:`, `WIP:`
- Body includes Feature ID

The patterns document is now ready for the coding agent to enforce consistency.